Pre-registration artifacts for the kill-gate experiments live here.
Lock rule: the filled template is committed BEFORE any model-comparison
metric is computed; the commit SHA is the immutable timestamp.
