# Local data, manifests, and private evaluation scripts — never commit
_local/

# NCR/ is the project's local-only source-of-truth folder (ground truth, shard membership,
# per-ablation results) — deliberately not version-controlled, local reference only.
NCR/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
env/
venv/
.venv/

# Jupyter
.ipynb_checkpoints/

# Common
.DS_Store
Thumbs.db
*.log

# Large data / zips
*.zip
