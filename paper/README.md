# AAAI-27 AISI draft — compile: `pdflatex main && bibtex main && pdflatex main && pdflatex main` (uses the OFFICIAL kit: `aaai2027.sty`/`aaai2027.bst`, copied here from `authorkit/AuthorKit27/`; full kit incl. `ReproducibilityChecklist.tex` lives in `authorkit/`).
Submission mode is set via `\usepackage[submission]{aaai2027}`; camera-ready drops the option. Do NOT add hyperref/geometry/font packages (forbidden by the kit). AISI keyword ("AISI: Computational Social Science and Humanities") is entered in OpenReview, not in LaTeX.
`\todo{}` items (101 in main.tex, incl. table/figure placeholder cells), grouped by the experiment/job that fills them:
- **W1** keypoint-floor sweep (breakpoint-0 rerun): Table B row B1; state tau_kp and raw-vs-filtered floor semantics (Method Stage 2).
- **W2/W3** one-shot frozen Shard-3 eval: Shard-3 counts (tab:dataset, protocol), Shard-3 columns in tab:verification, sensitivity heatmap panel (c), Conclusion test row.
- **W4/W9** retrieval baselines + retrains (A1–A8 in tab:retrieval, Recall@K/mAP) and transductive control (Method Stage 1, Limitations, Protocol).
- **W5** end-to-end survival audit: tab:survival cells, pipeline-figure survivor counts, Ethics coverage numbers, Limitations bound.
- **W6/W7/W8 + CPU re-scoring** verification ablations: rows B4–B12 (components, global-sim gate, homography/essential/MASt3R pose, learned classifier, alignment ablations) and B13/B14 matcher swaps; PR-AUC sweeps, bootstrap CIs, Holm-corrected McNemar column; fig:pose-components.
- **W10** frozen Jinchaji transfer run: Recall@10/15 + verification P/R/F1 (Transfer subsection, Intro contribution bullet, Conclusion).
- **W11/W12** synthetic exact-copy battery + pose-validity figure (Method, Limitations, commented Figure 1) and 3-seed retrain (Protocol); plus GPU/compute paragraph and VGGT checkpoint pinning.
- **Non-experiment (annotation/lookup/bib)**: Cohen's kappa + Unsure counts (dual-annotation), trace-type distribution + per-type pass rates, Findings 1–3 case studies + historian time-saved study (sessions Jul 16–21), negative-type audit, first-round candidate-pair count, *North China* issue count, YOLOv11 crop QC, round-two exclusion-list release, fig placeholders (pipeline/sensitivity/pose-components/hard-cases), and bib TODOs: `chen2024invisible` (fill from Du 2025 JCC reference list), NCR archive launch year, `net2023transductive` pages, `dias2013forests` DOI check, RoMa v2 venue.
